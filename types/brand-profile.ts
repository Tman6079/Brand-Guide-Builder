/**
 * Brand Profile extracted from a URL (visible page content only).
 * All fields are string; use "Not Provided" when missing.
 */
export interface BrandProfile {
  company_name: string;
  type_of_business: string;
  website: string;
  company_email: string;
  company_address: string;
  phone_number: string;
  business_hours: string;
  tone_of_voice: string;
  target_audience: string;
  customer_pain_points: string;
  brand_promise: string;
  brand_values: string;
  what_does_your_brand_do: string;
  what_makes_you_better_than_competitors: string;
  unique_selling_proposition: string;
  risks_of_inaction: string;
  call_to_action: string;
}
